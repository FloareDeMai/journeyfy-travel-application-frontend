import React from 'react'

function CityNames(props) {
    // const cityNames = props.citiesNames.map((city) => city.name)
    // const filteredCities = cityNames.filter((city) => !city.includes("Sector"))
    // console.log(filteredCities)
    return (
        <div>
        {/* {filteredCities.map((city) => <li>{city}</li>)} */}
    </div>
               
    )
}

export default CityNames
